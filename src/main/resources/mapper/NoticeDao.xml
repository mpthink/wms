<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.think.wms.dao.NoticeDao">    
    
    <insert id="insertNotice">
    	insert into
    		wms_notice(notice_title,notice_content,user_id)
    	values(#{noticeTitle},#{noticeContent},#{userId})
    </insert>
    
	<delete id="deleteNotice" parameterType="int">
		delete
			from wms_notice
		where
			notice_id=#{noticeId}
	</delete>
    
    <update id="updateNotice">
    	update wms_notice
    	<trim prefix="set" suffixOverrides=",">
    		<if test="noticeTitle != null">notice_title=#{noticeTitle},</if>
			<if test="noticeContent != null">notice_content=#{noticeContent},</if>
			<if test="userId != null">user_id=#{userId},</if>
		</trim>	
    	where
    		notice_id=#{noticeId}
    </update>
    
	<select id="queryByUserId" resultType="Notice" parameterType="long">
        select 
			notice.notice_id,
			notice.notice_title,
			notice.notice_content,
			notice.notice_create_time,
			user.user_id "user.user_id",
			user.user_name "user.user_name",
			user.user_realname "user.user_realname"
        from 
        	wms_notice notice
        left join wms_user user on notice.user_id = user.user_id
        where
        	notice.user_id=#{userId}
    </select>
    
	<select id="queryAll" resultType="Notice">
        select 
			notice.notice_id,
			notice.notice_title,
			notice.notice_content,
			notice.notice_create_time,
			user.user_id "user.user_id",
			user.user_name "user.user_name",
			user.user_realname "user.user_realname"
        from 
        	wms_notice notice
        left join wms_user user on notice.user_id = user.user_id
    </select>
</mapper>